---
# File: vars/Ubuntu.yml - additional variables for nomad (on Ubuntu)

nomad_ubuntu_pkg: "{{ nomad_version }}_linux_amd64.zip"
nomad_ubuntu_url: "{{ nomad_zip_url }}"
nomad_ubuntu_sha256: "{{ nomad_zip_sha256 }}"

nomad_ubuntu_os_packages:
  - git
  - unzip
